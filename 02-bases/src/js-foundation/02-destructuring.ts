const {USERNAME, COMPUTERNAME} = process.env
// console.table({USERNAME, COMPUTERNAME});

export const characters = ['Goku', 'Vegeta', 'Trunks'];
const [g, v, t] = characters;
// console.table({g, v, t});